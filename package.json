{
  "name": "brew-update-manager",
  "version": "1.1.1",
  "private": true,
  "author": "Tzoharlary",
  "license": "MIT",
  "description": "macOS desktop app for Homebrew update management",
  "main": "electron/main.js",
  "scripts": {
    "start-app": "electron .",
    "generate-icon": "/usr/local/bin/python3 tools/generate_app_icon.py",
    "pack": "electron-builder --dir --publish never",
    "build": "node tools/run-quick-build.js",
    "build:release": "node tools/run-release-build.js"
  },
  "devDependencies": {
    "electron": "35.1.5",
    "electron-builder": "26.0.12"
  },
  "build": {
    "appId": "com.tzoharlary.brewupdatemanager",
    "productName": "Homebrew Update Manager",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "electron/**/*",
      "renderer/**/*",
      "backend/**/*",
      "tools/**/*",
      "package.json",
      "README.md",
      "LICENSE"
    ],
    "asarUnpack": [
      "backend/**",
      "tools/**"
    ],
    "dmg": {
      "writeUpdateInfo": false
    },
    "mac": {
      "icon": "build/icon.icns",
      "category": "public.app-category.utilities",
      "target": [
        {
          "target": "dmg",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ],
      "artifactName": "${productName}-${version}-${arch}.${ext}",
      "hardenedRuntime": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist"
    }
  }
}
